source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

rails_version = ENV['RAILS_VERSION']
gem 'activerecord', rails_version

# Specify your gem's dependencies in newebpay-rails.gemspec
gemspec
